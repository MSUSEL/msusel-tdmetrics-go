---
description: Java abstractor — Steps 1–2 done; Step 3 (enums) handoff and file pointers
globs: javaAbstractor/**/*,testData/java/**/*,.agents/planning/2026-05-01-java-abstractor-completion/**/*
alwaysApply: false
---

# Java abstractor (MSUSEL TDD)

PhD tooling: follow repo **`AGENTS.md`** (plan → review → implement; no `git add` unless the researcher asks).

## Done: Steps 1 and 2

- **Step 1 — Robustness:** `Abstractor.addTypeDesc` / `addTypeDescImpl` and `addDeclaration` / `addTypeDeclaration` (try/catch, `getTypeDeclaration()`, wildcards incl. lower bounds w/ `Object` as unbounded, anonymous/local, annotation → `notice` + `objectDesc`, `<nulltype>` guard). `Analyzer` null-safe field usage and non-null `invokes`. `processPendingMetrics` batching to avoid `ConcurrentModificationException`.
- **Step 2 — External types:** `Baker.basicForBoxedOrString`. `Abstractor.addExternalStub` (stub `InterfaceDecl` cache by erasure QN, `InterfaceInst` + `setRefForElem` for parameterized JDK types). Shadow / unresolved / failed resolution → `addExternalStub`, not raw `objectDesc` for those paths.

Canonical checklist and narrative: **`.agents/planning/2026-05-01-java-abstractor-completion/implementation/plan.md`** (“Completion status” at top).

## Next: Step 3 (enum completion)

Read **`implementation/plan.md` § Step 3** unchanged. Code touchpoints:

- **`Abstractor.addEnum`** / `addObjectDecl` for `CtEnum` — enum constants as **`Value`** in package; struct / `$value` field; user methods (not `values()` / `valueOf()` noise per plan).
- **`addDeclaration`**: ensure **`CtEnum`** ordering vs **`CtClass`** remains correct.
- Tests: plan calls for **`test1006`** (create under `testData/java/` + `AppTests` hook) with expected YAML per `genFeatureDef.md`.

## Tests / hygiene

- Prefer **no `System.out.println`** in small `Tester` / `test100N` fixtures (Spoon expands JDK).
- Full **`mvn test`**: researcher may still be fixing **`MetricsTests`** YAML and Maven **`AppTests.test0001` / `test0002`** goldens.
- Fixtures: **`test1004`** (Step 1), **`test1005`** (Step 2), **`RobustnessTests`**.

## Design references

- Output schema: **`docs/genFeatureDef.md`**
- Gap list (partially stale vs Steps 1–2): **`research/gap-analysis.md`**
- Architecture snapshot: **`research/current-state.md`**
- Summary: **`summary.md`**
