package $builtin {
  path: $builtin;
}

package main {
  path: command-line-arguments;

  class dat<any T>:nested<any U> {
    T X;
    U Y;
  }
  inst dat<string>:nested<int>

  dat<any T>(T x, int y)
  inst dat<string>(string x, int y);
  foo<any T>(T z)
  inst foo<dat<string>:nested<int>>(dat<string>:nested<int> z);
  main();
}
