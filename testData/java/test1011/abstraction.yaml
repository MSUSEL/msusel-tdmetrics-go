{
  language: java,
  abstracts: [
    { name: bar,     signature: 3 }, # 1. bar( x int )
    { name: baz,     signature: 2 }, # 2. baz( value int )
    { name: execute, signature: 1 }, # 3. execute()
  ],
  arguments: [
    { name: value, type: basic1 }, # 1. value int
    { name: x,     type: basic1 }, # 2. x int
  ],
  basics: [ int ],
  interfaceDecls: [
    { # 1. Foo.funky{ execute() }
      name: funky, package: 1, interface: 2,
      loc: 10, nest: object1,
    },
  ],
  interfaceDescs: [
    { abstracts: [ 1, 2 ], pin: object1 }, # 1. interface{ bar( x int ); baz( value int ) }
    { abstracts: [ 3 ],    pin: object1 }, # 2. interface{ execute() }
  ],
  methods: [
    { # 1. Foo.bar( x int )
      name: bar, package: 1, receiver: 1, signature: 3,
      loc: 2, metrics: 1, vis: public,
    },
    { # 2. Foo.baz( value int )
      name: baz, package: 1, receiver: 1, signature: 2,
      loc: 8, vis: public,
    },
  ],
  metrics: [
    { # 1. Foo.bar( x int )
      loc: 2, codeCount: 5, complexity: 1, indents: 4, lineCount: 5,
      invokes: [
        abstract3, # execute()
        method2,   # Foo.baz( value int )
      ],
      reads: [
        basic1,         # int
        interfaceDecl1, # Foo.funky
        object1,        # Foo
      ],
    },
  ],
  objects: [
    { # 1. Foo{}{ bar( x int ); baz( value int ) }
      name: Foo, package: 1, data: 1, interface: 1,
      loc: 1, vis: public,
      methods: [ 1, 2 ],
      nested: [ interfaceDecl1 ],
    },
  ],
  packages: [
    {
      name: "<unnamed>",
      objects: [ 1 ],
      interfaces: [ 1 ],
      methods: [ 1, 2 ],
    },
  ],
  signatures: [
    {               }, # 1. func()
    { params: [ 1 ] }, # 2. func( value int )
    { params: [ 2 ] }, # 3. func( x int )
  ],
  structDescs: [
    { }, # 1. struct{}
  ],
  locs: { 1: ClassesFromSource.java },
}
