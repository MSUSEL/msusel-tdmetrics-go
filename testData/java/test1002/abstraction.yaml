{
  language: java,
  abstracts: [
    { name: $get, signature: 3 }, # 1. func $get(index int) int
    { name: $get, signature: 4 }, # 2. func $get(index int) T
    { name: $len, signature: 2 }, # 3. func $len() int
    { name: $set, signature: 5 }, # 4. func $set(index int, value int)
    { name: $set, signature: 6 }, # 5. func $set(index int, value T)
    { name: bar,  signature: 7 }, # 6. func bar(x int, y int) int
    { name: baz,  signature: 1 }, # 7. func baz()
    { name: cat,  signature: 8 }, # 8. func cat(t $Array<int> ...)
  ],
  arguments: [
    {              type: basic1         }, # 1. <unnamed> int
    {              type: typeParam1     }, # 2. <unnamed> T
    { name: index, type: basic1         }, # 3. index int
    { name: t,     type: interfaceInst1 }, # 4. t $Array<int>
    { name: value, type: basic1         }, # 5. value int
    { name: value, type: typeParam1     }, # 6. value T
    { name: x,     type: basic1         }, # 7. x int
    { name: y,     type: basic1         }, # 8. y int
  ],
  basics: [ int ],
  interfaceDecls: [
    { # 1. $Array<T>{ $get(index int) T; $len() int; $set(index int, value T) }
      name: $Array, package: 1, interface: 2,
      typeParams: [ 1 ],
    },
  ],
  interfaceDescs: [
    { # 1. interface{ $get(index int) int; $len() int; $set(index int, value int) }
      abstracts: [ 1, 3, 4 ],
    },
    { # 2. interface{ $get(index int) T; $len() int; $set(index int, value T) }
      abstracts: [ 2, 3, 5 ],
    },
    { # 3. interface{ bar(x int, y int) int; baz(); cat(t $Array<int> ...) }
      abstracts: [ 6, 7, 8 ], pin: object1,
    },
    { # 4. any
      abstracts: [ ],
    },
  ],
  interfaceInsts: [
    { # 1. $Array<int>{ $get(index int) int; $len() int; $set(index int, value int) }
      generic: 1, instanceTypes: [ basic1 ], resolved: 1,
    },
  ],
  methods: [
    { # 1. bar(x int, y int) int
      name: bar, package: 5, receiver: 1, signature: 7,
      loc:  4, metrics: 1,
    },
    { # 2. baz()
      name: baz, package: 5, receiver: 1, signature: 1,
      loc:  8, metrics: 2,
    },
    { # 3. cat(t $Array<int> ...)
      name: cat, package: 5, receiver: 1, signature: 8,
      loc: 12, metrics: 3,
    },
  ],
  metrics: [
    { loc:  4, codeCount: 3, complexity: 1, indents: 3, lineCount: 3 },
    { loc:  8, codeCount: 3, complexity: 1, indents: 3, lineCount: 3 },
    { loc: 12, codeCount: 3, complexity: 1, indents: 3, lineCount: 3 },
  ],
  objects: [
    { # 1. Foo{}{ bar(x int, y int) int; baz(); cat(t $Array<int> ...) }
      name: Foo, package: 5, data: 1, interface: 3,
      methods: [ 1, 2, 3 ],
      loc: 3, vis: public,
    },
  ],
  packages: [
    {
      name: $builtin,
      interfaces: [ 1 ],
    },
    { name: "<unnamed>" },
    { name: testData },
    { name: testData.java },
    {
      name: testData.java.test1002,
      objects: [ 1 ],
      methods: [ 1, 2, 3 ],
    },
  ],
  signatures: [
    {                                  }, # 1. func()
    {                   results: [ 1 ] }, # 2. func() int
    { params: [ 3 ],    results: [ 1 ] }, # 3. func(index int) int
    { params: [ 3 ],    results: [ 2 ] }, # 4. func(index int) T
    { params: [ 3, 5 ]                 }, # 5. func(index int, value int)
    { params: [ 3, 6 ]                 }, # 6. func(index int, value T)
    { params: [ 7, 8 ], results: [ 1 ] }, # 7. func(x int, y int) int
    { params: [ 4 ],    variadic: true }, # 8. func(t $Array<int> ...)
  ],
  structDescs: [
    { }, # 1. ???
  ],
  typeParams: [
    { name: T, type: interfaceDesc4 }, # 1. T ???
  ],
  locs: {
    1: ClassesFromSource.java,
  },
}
