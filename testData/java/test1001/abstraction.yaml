{
  language: java,
  abstracts: [
    { name: $get, signature: 3 }, # 1. $get(index int) int
    { name: $get, signature: 4 }, # 2. $get(index int) <T>
    { name: $len, signature: 1 }, # 3. $len() int
    { name: $set, signature: 5 }, # 4. $set(index int, value int)
    { name: $set, signature: 6 }, # 5. $set(index int, value <T>)
  ],
  arguments: [
    {              type: basic1     }, # 1. <unnamed> int
    {              type: object1    }, # 2. <unnamed> Foo
    {              type: typeParam1 }, # 3. <unnamed> <T>
    { name: index, type: basic1     }, # 4. index int
    { name: value, type: basic1     }, # 5. value int
    { name: value, type: typeParam1 }, # 6. value <T>
  ],
  basics: [ int ],
  fields: [
    { name: bar, type: basic1,         vis: public }, # 1. bar int
    { name: baz, type: interfaceInst1, vis: public }, # 2. baz $Array<int>
  ],
  interfaceDecls: [
    { # 1. $Array<T>{ $set(???); ??? }
      name: $Array, interface: 2, typeParams: [ 1 ],
    },
  ],
  interfaceDescs: [
    { abstracts: [ 1, 3, 4 ] },       # 1. ???
    { abstracts: [ 2, 3, 5 ] },       # 2. ???
    { abstracts: [ ] },               # 3. any
    { abstracts: [ ], pin: object1 }, # 4. any # TODO: If interface is "any", then there is no need to pin.
  ],
  interfaceInsts: [
    { # 1. $Array<int>{ ??? }
      generic: 1, resolved: 1,
      instanceTypes: [ basic1 ],
    },
  ],
  methods: [
    { # 1. Foo() Foo # TODO: Add additional hint to indicate constructor. If we can detect a default constructor, maybe we don't add it (check how Java does constructors)
      name: Foo, package: 4, receiver: 1, signature: 2, vis: public,
    },
  ],
  objects: [
    { # 1. Foo{ ??? }{ Foo() Foo }
      name: Foo, package: 4, data: 1, interface: 4, loc: 3, vis: public,
      methods: [ 1 ],
    },
  ],
  packages: [
    { name: "<unnamed>" },   # 1. <unnamed> package
    { name: testData },      # 2. testData package
    { name: testData.java }, # 3. testData.java package
    {                        # 4. testData.java.test1001 package
      name: testData.java.test1001,
      methods: [ 1 ], objects: [ 1 ],
    },
  ],
  signatures: [
    {                results: [ 1 ] }, # 1. func() int
    {                results: [ 2 ] }, # 2. func() Foo
    { params: [ 4 ], results: [ 1 ] }, # 3. func(index int) int
    { params: [ 4 ], results: [ 3 ] }, # 4. func(index int) <T>
    { params: [ 4, 5 ]              }, # 5. func(index int, value int)
    { params: [ 4, 6 ]              }, # 4. func(index int, value <T>)
  ],
  structDescs: [
    { fields: [ 1, 2 ] }, # 1. struct{ bar int; baz $Array<int> }
  ],
  typeParams: [
    { name: T, type: interfaceDesc3 }, # 1. <T> any ???
  ],
  locs: {
    1: ClassesFromSource.java,
  },
}
