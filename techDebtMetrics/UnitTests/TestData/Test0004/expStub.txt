package main {
   path: command-line-arguments;

   Animal: interface {
      Age() => int;
   };

   Bird: interface {
      Age() => int;
      Fly();
   };

   Mammal: interface {
      Age() => int;
      Temp() => float64;
   };

   Bat: interface {
      Age() => int;
      Fly();
      Temp() => float64;
   };

   Flier: interface {
      Fly();
   };

   main();
}
