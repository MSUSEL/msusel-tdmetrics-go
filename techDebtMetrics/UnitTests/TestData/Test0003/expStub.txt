package $builtin {
  path: $builtin;

  interface Pointer<T any> {
    implements: any;
    $deref() T;
  }
}

package main {
  path: command-line-arguments;

  interface Animal {
    implements: any;
    Pet();
  }

  class Cat {
    Name string;
    Pet();
  }

  Pet();
  main();
}
